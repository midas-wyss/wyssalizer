# faculty data frame

wyss_scholar <- vector(mode = "list")

##### CORE ####
wyss_scholar[[1]] <- tibble(id = "aizenberg_j", 
                            last_name = str_split(id, "_")[[1]][1],
                            initials = str_split(id, "_")[[1]][2],
                            type = "core",
                            scholar_id = "xKJyuMAAAAAJ",
                            pubmed_search = paste(last_name, initials),
                            year_joined = 2010)
wyss_scholar[[2]] <- tibble(id = "church_gm", 
                            last_name = str_split(id, "_")[[1]][1],
                            initials = str_split(id, "_")[[1]][2],
                            type = "core",
                            scholar_id = "SfDzdgEAAAAJ",
                            pubmed_search = paste(last_name, initials),
                            year_joined = 2010)
wyss_scholar[[3]] <- tibble(id = "collins_jj", 
                            last_name = str_split(id, "_")[[1]][1],
                            initials = str_split(id, "_")[[1]][2],
                            type = "core",
                            scholar_id = "te5DjPQAAAAJ",
                            pubmed_search = paste(last_name, initials),
                            year_joined = 2010)
wyss_scholar[[4]] <- tibble(id = "ingber_de", 
                            last_name = str_split(id, "_")[[1]][1],
                            initials = str_split(id, "_")[[1]][2],
                            type = "core",
                            scholar_id = "3hzhsK4AAAAJ",
                            pubmed_search = paste(last_name, initials),
                            year_joined = 2010)
wyss_scholar[[5]] <- tibble(id = "joshi_ns", 
                            last_name = str_split(id, "_")[[1]][1],
                            initials = str_split(id, "_")[[1]][2],
                             type = "core",
                             scholar_id = "_O23UeQAAAAJ",
                            pubmed_search = paste(last_name, initials),
                            year_joined = 2010)
wyss_scholar[[6]] <- tibble(id = "lewis_ja", 
                            last_name = str_split(id, "_")[[1]][1],
                            initials = str_split(id, "_")[[1]][2],
                             type = "core",
                             scholar_id = "Wdny3bkAAAAJ",
                            pubmed_search = paste(last_name, initials),
                            year_joined = 2012)
wyss_scholar[[7]] <- tibble(id = "mitragotri_s", 
                            last_name = str_split(id, "_")[[1]][1],
                            initials = str_split(id, "_")[[1]][2],
                             type = "core",
                             scholar_id = "3_77zesAAAAJ",
                            pubmed_search = paste(last_name, initials),
                            year_joined = 2018)
wyss_scholar[[7]] <- tibble(id = "mooney_dj", 
                            last_name = str_split(id, "_")[[1]][1],
                            initials = str_split(id, "_")[[1]][2],
                            type = "core",
                            scholar_id = "ZflJqeUAAAAJ",
                            pubmed_search = paste(last_name, initials),
                            year_joined = 2010)
wyss_scholar[[8]] <- tibble(id = "nagpal_r", 
                            last_name = str_split(id, "_")[[1]][1],
                            initials = str_split(id, "_")[[1]][2],
                            type = "core",
                            scholar_id = "a3ZGDsoAAAAJ",
                            pubmed_search = paste(last_name, initials),
                            year_joined = 2010)
wyss_scholar[[9]] <- tibble(id = "parker_kk", 
                            last_name = str_split(id, "_")[[1]][1],
                            initials = str_split(id, "_")[[1]][2],
                            type = "core",
                            scholar_id = "Ut0YbEoAAAAJ",
                            pubmed_search = paste(last_name, initials),
                            year_joined = 2010)
wyss_scholar[[11]] <- tibble(id = "silver_pa", 
                             last_name = str_split(id, "_")[[1]][1],
                            initials = str_split(id, "_")[[1]][2],
                            type = "core",
                            scholar_id = "X-8ViBEAAAAJ",
                            pubmed_search = paste(last_name, initials),
                            year_joined = 2010)
wyss_scholar[[12]] <- tibble(id = "walsh_cj", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "core",
                             scholar_id = "sKSTKAoAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2011)

wyss_scholar[[14]] <- tibble(id = "weitz_da", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "R1cfTCoAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2011)
wyss_scholar[[16]] <- tibble(id = "wood_rj", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "core",
                             scholar_id = "FTYtHb4AAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2010)
wyss_scholar[[17]] <- tibble(id = "yin_p", 
                             last_name = str_split(id, "_")[[1]][1],
                            initials = str_split(id, "_")[[1]][2],
                            type = "core",
                            scholar_id = "GyGK1DAAAAAJ",
                            pubmed_search = paste(last_name, initials),
                            year_joined = 2010)


##### ASSOCIATE #####
wyss_scholar[[18]] <- tibble(id = "awad_l", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "nJOcum8AAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2016)

wyss_scholar[[20]] <- tibble(id = "bertoldi_k", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "Jt1sCk8AAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2017)
wyss_scholar[[21]] <- tibble(id = "bhatia_sn", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "L-SoYz8AAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2018)
wyss_scholar[[22]] <- tibble(id = "bonato_p", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "-YJ5ZlUAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)

wyss_scholar[[24]] <- tibble(id = "chen_cs", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "-6maX04AAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2013)
wyss_scholar[[25]] <- tibble(id = "duda_gn", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "D-AN9vQAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2013)

wyss_scholar[[30]] <- tibble(id = "levin_m", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "luouyakAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2017)


##### STAFF #####
wyss_scholar[[37]] <- tibble(id = "camacho_dm", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "JjKJpygAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2016)
wyss_scholar[[38]] <- tibble(id = "super_m", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "N59CN60AAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2010)
wyss_scholar[[39]] <- tibble(id = "stirling_la", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "RO4jc9EAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2010)
wyss_scholar[[40]] <- tibble(id = "werfel_j", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "ZffyJucAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2010)
wyss_scholar[[41]] <- tibble(id = "galloway_kc", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "EnzYASIAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)
wyss_scholar[[42]] <- tibble(id = "hatton_bd", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "RYi6Yh8AAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2013)
wyss_scholar[[43]] <- tibble(id = "kosuri_s", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "ev8hXyEAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2013)
wyss_scholar[[44]] <- tibble(id = "uzun_o", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "ryJE0P4AAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2013)
wyss_scholar[[45]] <- tibble(id = "weaver_jc", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "PV2jwkUAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2013)
wyss_scholar[[46]] <- tibble(id = "inverso_sa", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "RKvrOZ8AAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2014)
wyss_scholar[[47]] <- tibble(id = "kim_p", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "fiB3gqMAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2014)
wyss_scholar[[48]] <- tibble(id = "lee_jh", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "DjdxZ24AAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2014)
wyss_scholar[[49]] <- tibble(id = "stafford_ag", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "LxyxBzQAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2014)
wyss_scholar[[50]] <- tibble(id = "vigneault_f", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "5M2w2ScAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2014)
wyss_scholar[[51]] <- tibble(id = "ahn_by", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "TKGhPXwAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2015)
wyss_scholar[[52]] <- tibble(id = "henry_o", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "Yw3uwZIAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2015)
wyss_scholar[[53]] <- tibble(id = "korin_n", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "tdO81DkAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2015)
wyss_scholar[[54]] <- tibble(id = "novak_r", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "_4uGgkgAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2015)
wyss_scholar[[55]] <- tibble(id = "asbeck_at", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "vQ1M5CIAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2016)
wyss_scholar[[56]] <- tibble(id = "derossi_smm", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "C4aIo9YAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2016)
wyss_scholar[[57]] <- tibble(id = "leslie_dc", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "XvxKPz4AAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2016)
wyss_scholar[[58]] <- tibble(id = "pruitt_bw", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "kAoP9jUAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2016)
wyss_scholar[[59]] <- tibble(id = "perrault_s", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "3tZwt_UAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2017)
wyss_scholar[[60]] <- tibble(id = "valentine_ad", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "ALs9A78AAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2017)
wyss_scholar[[61]] <- tibble(id = "vogt_dm", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "xBX234cAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2017)
wyss_scholar[[62]] <- tibble(id = "alvarenga_j", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "mZoQiW0AAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2018)
wyss_scholar[[63]] <- tibble(id = "kelsic_ed", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "ggBcJnEAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2018)
wyss_scholar[[64]] <- tibble(id = "kohman_re", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "1F7Wl9sAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2018)
wyss_scholar[[65]] <- tibble(id = "lee_jw", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "Mu7NXDwAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2018)
wyss_scholar[[66]] <- tibble(id = "levy_o", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "vwSqK9QAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2018)
wyss_scholar[[67]] <- tibble(id = "jolly_p", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "-Vjx5isAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2018)
wyss_scholar[[68]] <- tibble(id = "brown_cw", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "hhN4-0MAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2019)
wyss_scholar[[69]] <- tibble(id = "karavas_n", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "M0gjGmoAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2019)
wyss_scholar[[70]] <- tibble(id = "slomovic_s", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "P_swsagAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2019)
wyss_scholar[[71]] <- tibble(id = "wiegand_dj", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "oHqkIAwAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2019)
wyss_scholar[[72]] <- tibble(id = "yang_d", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "9TWsq-MAAAAJ",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2019)


# folks with no google scholar id
wyss_scholar[[10]] <- tibble(id = "shih_w", 
                             last_name = "shih", 
                             first_name = "william",
                             initials = str_split(id, "_")[[1]][2],
                             type = "core",
                             scholar_id = "",
                             pubmed_search = "shih w",
                             year_joined = 2010)
wyss_scholar[[13]] <- tibble(id = "walt_dr", 
                             last_name = "walt", 
                             first_name = "david",
                             initials = str_split(id, "_")[[1]][2],
                             type = "core",
                             scholar_id = "",
                             pubmed_search = "walt dr",
                             year_joined = 2018)
wyss_scholar[[15]] <- tibble(id = "whitesides_gm", 
                             last_name = "whitesides", 
                             first_name = "george",
                             initials = str_split(id, "_")[[1]][2],
                             type = "core",
                             scholar_id = "",
                             pubmed_search = "whitesides gm",
                             year_joined = 2010)
wyss_scholar[[19]] <- tibble(id = "bechtold_m", 
                             last_name = "bechtold", 
                             first_name = "martin",
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "",
                             pubmed_search = "",
                             year_joined = 2014)
wyss_scholar[[23]] <- tibble(id = "chaikof_el", 
                             last_name = "chaikof", 
                             first_name = "elliot",
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "",
                             pubmed_search = "chaikof el",
                             year_joined = 2011)
wyss_scholar[[26]] <- tibble(id = "goldfield_ec", 
                             last_name = "goldfield", 
                             first_name = "gene",
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "",
                             pubmed_search = "goldfield ec",
                             year_joined = 2011)
wyss_scholar[[27]] <- tibble(id = "hoberman_c", 
                             last_name = "hoberman", 
                             first_name = "chuck",
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "",
                             pubmed_search = "hoberman c",
                             year_joined = 2016)
wyss_scholar[[28]] <- tibble(id = "hoerstrup_sp", 
                             last_name = "hoerstrup", 
                             first_name = "simon",
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "",
                             pubmed_search = "hoerstrup sp",
                             year_joined = 2014)
wyss_scholar[[29]] <- tibble(id = "howe_rd", 
                             last_name = "howe", 
                             first_name = "robert",
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "",
                             pubmed_search = "howe rd",
                             year_joined = 2013)
wyss_scholar[[31]] <- tibble(id = "warren_s", 
                             last_name = "warren", 
                             first_name = "shaw",
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "",
                             pubmed_search = "warren hs",
                             year_joined = 2016)
wyss_scholar[[32]] <- tibble(id = "wong_wp", 
                             last_name = "wong", 
                             first_name = "wesley",
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "",
                             pubmed_search = "wong wp",
                             year_joined = 2011)
wyss_scholar[[33]] <- tibble(id = "wu_ct", 
                             last_name = "wu", 
                             first_name = "c.-ting",
                             initials = str_split(id, "_")[[1]][2],
                             type = "associate",
                             scholar_id = "",
                             pubmed_search = "wu ct",
                             year_joined = 2017)


wyss_scholar[[73]] <- tibble(id = "aizenberg_ma", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2010)
wyss_scholar[[74]] <- tibble(id = "niemi_jb", 
                             last_name = str_split(id, "_")[[1]][1],
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2010)
wyss_scholar[[76]] <- tibble(id = "terry_r", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2010)
wyss_scholar[[77]] <- tibble(id = "way_jc", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2010)
wyss_scholar[[78]] <- tibble(id = "blough_rt", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2011)
wyss_scholar[[79]] <- tibble(id = "conway_n", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2011)
wyss_scholar[[80]] <- tibble(id = "doherty_e", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2011)
wyss_scholar[[81]] <- tibble(id = "mughees_k", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2011)
wyss_scholar[[82]] <- tibble(id = "watters_al", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2011)
wyss_scholar[[83]] <- tibble(id = "ali_o", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)

wyss_scholar[[84]] <- tibble(id = "bahinsky_a", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)
wyss_scholar[[85]] <- tibble(id = "hamilton_ga", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)
wyss_scholar[[86]] <- tibble(id = "cartwright_mj", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)
wyss_scholar[[87]] <- tibble(id = "domansky_k", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)
wyss_scholar[[88]] <- tibble(id = "domansky_k", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)
wyss_scholar[[89]] <- tibble(id = "lesniewski-laas_n", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)
wyss_scholar[[90]] <- tibble(id = "li_c", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)
wyss_scholar[[91]] <- tibble(id = "orborne_j", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)
wyss_scholar[[92]] <- tibble(id = "roberts_k", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)
wyss_scholar[[93]] <- tibble(id = "sallum_h", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)
wyss_scholar[[94]] <- tibble(id = "wozniak_av", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)
wyss_scholar[[95]] <- tibble(id = "freidsham_c", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)
wyss_scholar[[96]] <- tibble(id = "korensky_b", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)
wyss_scholar[[97]] <- tibble(id = "korensky_b", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2012)
wyss_scholar[[98]] <- tibble(id = "dubrovsky_z", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2013)
wyss_scholar[[99]] <- tibble(id = "karpelson_m", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2013)
wyss_scholar[[100]] <- tibble(id = "dyer_rj", 
                             last_name = str_split(id, "_")[[1]][1], 
                             initials = str_split(id, "_")[[1]][2],
                             type = "staff",
                             scholar_id = "",
                             pubmed_search = paste(last_name, initials),
                             year_joined = 2014)
wyss_scholar[[101]] <- tibble(id = "fernandez-alcon_j", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2014)
wyss_scholar[[102]] <- tibble(id = "haney_sm", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2014)
wyss_scholar[[103]] <- tibble(id = "kanapathipillai_m", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2014)
wyss_scholar[[104]] <- tibble(id = "karabas_m", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2014)
wyss_scholar[[105]] <- tibble(id = "knodel_cl", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2014)
wyss_scholar[[106]] <- tibble(id = "levner_d", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2014)
wyss_scholar[[106]] <- tibble(id = "levner_d", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2014)
wyss_scholar[[107]] <- tibble(id = "turczyk_bm", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2014)
wyss_scholar[[108]] <- tibble(id = "bhatta_d", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2015)
wyss_scholar[[109]] <- tibble(id = "graveline_ar", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2015)
wyss_scholar[[110]] <- tibble(id = "gravelle_dc", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2015)
wyss_scholar[[111]] <- tibble(id = "grinthal_ae", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2015)
wyss_scholar[[111]] <- tibble(id = "grinthal_ae", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2015)
wyss_scholar[[112]] <- tibble(id = "kalish_dp", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2015)
wyss_scholar[[113]] <- tibble(id = "kesner_sb", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2015)
wyss_scholar[[113]] <- tibble(id = "patel_p", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2015)
wyss_scholar[[113]] <- tibble(id = "rottman_m", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2015)
wyss_scholar[[113]] <- tibble(id = "rottman_m", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2015)
wyss_scholar[[114]] <- tibble(id = "couture_a", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2016)
wyss_scholar[[115]] <- tibble(id = "dugas_d", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2016)
wyss_scholar[[116]] <- tibble(id = "bujanda_ig", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2016)
wyss_scholar[[116]] <- tibble(id = "greeley_g", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2016)
wyss_scholar[[117]] <- tibble(id = "hicks_w", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2016)
wyss_scholar[[118]] <- tibble(id = "prantil-baun_r", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2016)
wyss_scholar[[119]] <- tibble(id = "reilly_c", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2016)
wyss_scholar[[120]] <- tibble(id = "stilson_s", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2016)
wyss_scholar[[121]] <- tibble(id = "verma_a", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2016)
wyss_scholar[[122]] <- tibble(id = "davidsohn_n", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2017)
wyss_scholar[[123]] <- tibble(id = "dunne_ap", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2017)
wyss_scholar[[124]] <- tibble(id = "fitzgibbons_sa", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2017)
wyss_scholar[[125]] <- tibble(id = "monreal_aw", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2017)
wyss_scholar[[126]] <- tibble(id = "odonnel_k", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2017)
wyss_scholar[[127]] <- tibble(id = "schaus_t", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2017)
wyss_scholar[[128]] <- tibble(id = "wagner_d", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2017)
wyss_scholar[[129]] <- tibble(id = "waterhouse_a", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2017)
wyss_scholar[[130]] <- tibble(id = "wong_t", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2017)
wyss_scholar[[131]] <- tibble(id = "athanassiu_m", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2018)
wyss_scholar[[132]] <- tibble(id = "chalkiadaki_a", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2018)
wyss_scholar[[133]] <- tibble(id = "delahanty_a", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2018)
wyss_scholar[[134]] <- tibble(id = "dimitrakakis_n", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2018)
wyss_scholar[[134]] <- tibble(id = "dimitrakakis_n", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2018)
wyss_scholar[[135]] <- tibble(id = "imaizumi_k", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2018)
wyss_scholar[[136]] <- tibble(id = "macdonald_ji", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2018)
wyss_scholar[[137]] <- tibble(id = "eckert-erdheim_a", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2019)
wyss_scholar[[138]] <- tibble(id = "karkada_m", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2019)
wyss_scholar[[139]] <- tibble(id = "lansberry_g", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2019)
wyss_scholar[[140]] <- tibble(id = "murphy_p", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2019)
wyss_scholar[[141]] <- tibble(id = "orzel_d", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2019)
wyss_scholar[[142]] <- tibble(id = "perry_d", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2019)
wyss_scholar[[143]] <- tibble(id = "phipps_ns", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2019)
wyss_scholar[[144]] <- tibble(id = "rouleau_m", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2019)
wyss_scholar[[145]] <- tibble(id = "ryan_dl", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2019)
wyss_scholar[[146]] <- tibble(id = "seiler_b", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2019)
wyss_scholar[[147]] <- tibble(id = "tam_j", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2019)
wyss_scholar[[148]] <- tibble(id = "walsh_t", 
                              last_name = str_split(id, "_")[[1]][1], 
                              initials = str_split(id, "_")[[1]][2],
                              type = "staff",
                              scholar_id = "",
                              pubmed_search = paste(last_name, initials),
                              year_joined = 2019)










#####
wyss_scholar <- dplyr::bind_rows(wyss_scholar)

##### SAVE #####
save(file = paste0("data/", format(Sys.Date(), "%Y-%m-%d"), "_wyss_scholar_dataframe.RData"), wyss_scholar)


# names
# pubmed for other faculty (email for google scholar) -- cc don
# cumulative 








